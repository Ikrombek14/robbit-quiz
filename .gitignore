# Dependencies
node_modules/

# Build outputs
dist/
public/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# To'g'ridan-to'g'ri deploy skripti (server paroli, DB parol, JWT, admin) — commit qilinmaydi
deploy-direct.ps1

# Deploy artefaktlari
_deploy/
robbit-quiz.tgz

# Database (dev SQLite)
*.db
*.db-journal
*.sqlite
*.sqlite3

# Runtime uploads (foydalanuvchi yuklagan PDF/rasmlar)
backend/uploads/

# OS / editor
.DS_Store
Thumbs.db

# Roster CSV (shaxsiy ma'lumot) — repoga qo'shilmaydi
*.csv
